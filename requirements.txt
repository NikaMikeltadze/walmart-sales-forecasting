# Core
pandas
numpy
scikit-learn
matplotlib
# Imported directly (parallel per-series Prophet fits, model persistence)
# rather than relied on as a transitive scikit-learn dependency.
joblib

# Experiment tracking
mlflow
python-dotenv

# Tree-based models
xgboost
lightgbm

# Classical statistical models
statsmodels
pmdarima
prophet

# Deep learning models (N-BEATS + DLinear on darts; TFT on neuralforecast).
# N-BEATS uses darts NBEATSModel with fit(sample_weight=...) for the 5x holiday
# WMAE loss, which needs darts >= 0.30.
darts[torch]>=0.30

# Hyperparameter search (Optuna TPE study in the N-BEATS CV stage)
optuna

# Notebook execution
jupyter
